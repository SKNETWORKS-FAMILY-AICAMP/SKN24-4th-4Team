# 복수 보험사 동시 검색 후 비교표 생성
from utils.schemas import InsuranceState
from utils.comparison import build_comparison_table


def compare(state: InsuranceState) -> dict:
    # TODO: 복수 보험사 컬렉션 동시 검색 후 비교표 포맷팅
    return {"answer": ""}
